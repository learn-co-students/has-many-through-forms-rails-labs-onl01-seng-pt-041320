<%# categories#show page that links to all of the posts associated with a given category. %>
<%# categories show page should display all of the related posts %>

<ol>
<% @category.posts.each do |c| %>
    <li>
        <%= link_to(c.title, post_path(c)) %>
    </li>
<% end %>
</ol>
